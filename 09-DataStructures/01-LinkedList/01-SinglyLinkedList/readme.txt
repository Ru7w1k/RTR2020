# Singly Linked List

Files:
1. sll.h  => function and types declaration for linked list
2. sll.c  => actual implementation of functions for linked list
3. main.c => menu driven program to execute linked list functions

4. build.bat => contains the build command to compile, link and execute the program

