# Graph using adjecency list

Files:
1. graph.h  => function and types declaration for graph
2. graph.c  => actual implementation of functions for graph
3. main.c => menu driven program to execute graph functions

4. build.bat => contains the build command to compile, link and execute the program

